# Multimodal-Enterprise-RAG-MVP

MVP multimodal RAG pipeline 

# Running the code
Run the command `pip install -e .` first

Run `!python3 main.py`

The main method will prompt you to input the path. Input the correct path to the file you want to upload to ensure smooth running of the code


